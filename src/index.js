import { print } from "./scripts/main.js";

setTimeout(() => print(), 4000);

/* setTimeout(() => {
  document.getElementsByClassName("console")[0].onclick = () => {
    window.location.replace("/index2.html");
  };
}, 8000); */
